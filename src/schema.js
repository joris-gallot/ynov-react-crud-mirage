export const schema = {
  users: {
    type: 'users',
    fields: {
      name: 'string',
      // created: {
      //   type: 'date', // converts value to a Date object
      //   readOnly: true, // removes field for mutations
      // },
    },
  },
}
